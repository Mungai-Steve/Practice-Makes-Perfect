#include <stdio.h>

/**
 * main - program starts here
 *
 * return: program ends here
 */

int main(void)

{
	int length, n;

	printf("Enter the length of line:");
	scanf("%d", &length);
	if (n <= 0)	
	for (n = 0; n < length; n++)
	{
	putchar(95);
	}
	
	putchar('\n');
	return (0);
	
}
